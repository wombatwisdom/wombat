---
title: crash
kind: processor
---
import { Aside } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

Crashes the process using a fatal log message. The log message can be set using function interpolations described in  Bloblang queries which allows you to log the contents and metadata of messages.

```yml
# Config fields, showing default values
label: ""
crash: "" # No default (required)
```


